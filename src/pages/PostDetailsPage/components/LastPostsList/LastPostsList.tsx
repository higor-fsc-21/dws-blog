import React from "react";
import styles from "./LastPostsList.module.scss";

const LastPostsList: React.FC = () => {
  return (
    <div className={styles.lastPostsList}>{/* LastPostsList content */}</div>
  );
};

export default LastPostsList;

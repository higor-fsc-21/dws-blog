import React from "react";
import styles from "./MobileFilters.module.scss";

const MobileFilters: React.FC = () => {
  return (
    <div className={styles.mobileFilters}>{/* MobileFilters content */}</div>
  );
};

export default MobileFilters;

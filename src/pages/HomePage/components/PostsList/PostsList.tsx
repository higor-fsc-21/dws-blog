import React from "react";
import styles from "./PostsList.module.scss";

const PostsList: React.FC = () => {
  return <div className={styles.postsList}>{/* PostsList content */}</div>;
};

export default PostsList;
